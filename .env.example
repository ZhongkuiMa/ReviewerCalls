# All configuration is now via CLI arguments.
# See: python -m discover --help
#
# Example usage:
#   python -m discover --dry-run
#   python -m discover --search-provider serper --serper-key YOUR_KEY --repo owner/repo
