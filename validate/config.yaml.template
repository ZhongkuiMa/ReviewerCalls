# Copy to config.yaml and edit. config.yaml is gitignored.

# SSH tunnel to remote Ollama server (auto-managed)
ssh_tunnel:
  enabled: true
  host: "10.35.12.183"
  port: 22
  username: "zhongkui"
  remote_host: "localhost"
  remote_port: 11434
  local_port: 11434

ollama:
  host: "http://localhost:11434"
  model: "qwen2.5:7b"
  keep_alive: "30m"
  options:
    temperature: 0.1
    num_predict: 512
    num_ctx: 16384

validation:
  max_content_chars: 6000
  timeout_seconds: 60
  retry_count: 2
  retry_delay_seconds: 5
